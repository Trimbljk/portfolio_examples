---
AWSTemplateFormatVersion: '2010-09-09'

Resources:
  NginxEcrRepository:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: fargate/restricteduse_nginx

  AppEcrRepository:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: fargate/restricteduse_app
